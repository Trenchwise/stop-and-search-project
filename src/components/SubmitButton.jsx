import React from "react";

// Need to add event listener so it call api when button is clicked.

const SubmitButton = () => {
  return <button onClick={}>Submit</button>;
};

export default SubmitButton;
